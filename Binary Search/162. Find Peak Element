class Solution:
    def findPeakElement(self, nums: List[int]) -> int:
        # countmax=0
        # for i in range (len(nums)-1):
        #     if nums[i]>countmax:
        #         countmax=nums[i]
        # return nums.index(countmax)
        return nums.index(max(nums))
