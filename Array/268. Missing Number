https://leetcode.com/problems/missing-number/description/

class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        i=0
        while (i<=len(nums)):
            if i in nums:
                i+=1
            else: 
                return i
